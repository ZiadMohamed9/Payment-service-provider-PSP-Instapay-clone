package com.psp.instapay.model.enums;

/**
 * Enum representing the roles of users in the system.
 * Defines the access levels and permissions for each role.
 */
public enum Role {
    /**
     * Role assigned to regular users of the system.
     */
    USER,

    /**
     * Role assigned to administrators of the system.
     */
    ADMIN
}